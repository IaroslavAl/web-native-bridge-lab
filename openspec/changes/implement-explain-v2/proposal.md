# Explain v2: a truthful guided demonstration

## Why

The accepted foundation at `778fbb7738212ab817a23aea80685dc1d49cfd78` proves generic native HTTP and same-installed-app web replacement. Its English scenario selector and Diagnostics-first result placement do not explain the benefit to non-developer colleagues. The owner selected and authorized the bright Russian Explain v2 direction; implementation approval is not pending.

## What Changes

- Replace the main demo with a compact catalog → independently served update → quote journey and persistent request/response route.
- Gate the new quote action by the actually loaded B build, not a client-side flag. Distinguish page loading from business HTTP.
- Bind outcomes to real bridge replies/web interpretation, expose truthful errors/retry and actual web/native identity.
- Move existing technical controls to an explicit separate engineering surface; retain their assertions and transport/security harnesses.
- Adapt the existing installed-shell A/B proof and add semantic/layout/update-error acceptance. No simulator or implementation runs belong to this planning change authoring phase.

## Capabilities

### New Capabilities
- `explain-demo`: understandable, event-grounded Russian demo and truthful web-only update experience.

### Modified Capabilities
- None at the protocol level. The new capability adds stricter presentation requirements over existing WS-01/02/03 without weakening them.

## Impact

Web App/bootstrap/styles/interpreters and tests; generic native shell/load-state presentation and focused adapter/UI tests; existing verification runner and README/evidence. See `docs/agent/EXPLAIN_V2_PLAN.md` for exclusive serial ownership. No business endpoints/models in native, wire changes, API/CSP/origin/limit changes, backend redesign, new dependencies by default, or production/device/cloud scope.

## Review and source of truth

Independent same-card spec review precedes implementation. The old `add-bridge-lab` change is still active and canonical specs are empty at this baseline; this packet adds only `explain-demo` and does not pretend to modify an already-synced canonical capability. Do not edit/archive the old change or replay its graph here. Later coordinator-owned lifecycle reconciliation must preserve and sync the accepted baseline before closing the follow-up; green CLI validation alone is not acceptance.
