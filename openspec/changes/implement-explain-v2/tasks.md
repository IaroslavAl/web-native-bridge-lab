## 1. Reviewed design baseline

- [x] 1.1 Inspect accepted baseline, current client/shell/tests and selected simulated design; record honest event boundaries and authorization.
- [x] 1.2 Write bounded mission/design/delta and serial ownership/test plan; retain non-production reference.
- [x] 1.3 Validate with project-local OpenSpec 1.12.0 and obtain independent same-card spec review; coordinator alone releases later execution.
- [x] 1.4 Address first-review R1 in docs/spec: fixed installed footer ingress, W bootstrap contract, N-complete Stage5 prerequisites and EX-08 entry/return/retry scenarios; preserve rejected head/evidence. Independent re-review remains part of 1.3.

## 2. W — Web journey

- [x] 2.1 Add failing EX-01–05 and EX-07 semantic/data/history tests, extract technical controls without dropping assertions; test D1 diagnostics mode selection/history isolation in A/B and retain hostile-body literal-text/no-injected-elements assertions (EX-08).
- [x] 2.2 Implement coarse real-event states, structured web interpretation, Russian errors/retry and one action/result surface.
- [x] 2.3 Implement actual loaded identity/reload comparison, A/B capability gating, cold B and honest repeat; static bootstrap fallback.
- [x] 2.4 Run W command gates and inspect actual A/B output identity; request independent same-card review with exact head and pending integrated gates disclosed.

## 3. N — Native shell and shared harness

- [x] 3.1 Add focused tests for generic load/recovery/native identity and D5 closed destination selection/failure retry/return; implement light shell, footer context menu and equivalent accessibility actions without changing wire, trust or lifecycle ordering. Update README activation/return/reload instructions.
- [x] 3.2 Adapt shared SameBinaryTests: Stage5 uses actual footer long-press/action → production Diagnostics → mode-preserving native reload → all existing body/category/timeout/slow-only-cancel assertions → return to B before matrix-observed. Keep Stage2/3 raw-root reload flow and all trust assertions. Existing runner needs no ingress change; do not defer an N prerequisite to R.
- [x] 3.3 Run N build/analyze, Stage5 and Stage4 commands; verify owned cleanup and request independent same-card review.

## 4. R — Runtime evidence

- [x] 4.1 Add tested simulator-explain-v2 mode by reusing existing proof/cleanup; cover unchanged update, real B, failures/retry and missing-asset recovery.
- [x] 4.2 Verify default viewport, scroll-top, reduced-motion, assistive/large-text and actual WK safe-area layout; retain screenshots and limitations.
- [x] 4.3 Run R command gates, record diff-based additional regression selection, full installed-file identity/backend attribution and teardown; request same-card review.

## 5. Independent acceptance and later closure

- [ ] 5.1 Fresh Astra/xhigh verifier independently checks exact candidate with requirements/evidence matrix; no author self-acceptance.
- [ ] 5.2 Coordinator reconciles reviewed source/tests/specs and active baseline lifecycle without mutating old mission graph; only then prepares final integration and owner RC handoff.
- [ ] 5.3 Record owner product acceptance separately; do not equate a validated plan, technical gate or mockup QA with accepted Explain UI.
