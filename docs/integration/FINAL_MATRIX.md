# Integrated candidate: requirement / scenario / proof layer

This is the final implementation coverage map, not approval or owner acceptance. The original obligations remain in [verification plan](../verification-plan.md), active OpenSpec specs and [protocol v1](../../protocol/v1/README.md). Historical Stage1–5 reports describe observations at their own heads; their then-pending lists are reconciled below, not silently waived. Final candidate commands and observed exact source SHA belong in `final-candidate-evidence.json` (when produced) and the same-card review handoff. Until that record exists and all required gates pass, final-source regression is pending.

## Executable command key and layer

Run commands separately, serializing fixed ports / dedicated Simulators. `scripts/verify unit` installs pinned dependencies from lockfiles into this checkout. Run from the repository root; no global install.

- U: `scripts/verify unit`. Backend real temporary HTTP/process tests; native network/clock doubles (except explicitly identified URLSession/real timer tests); iOS core doubles; web DOM/client mocked boundary; strict schema/OpenSpec, TypeScript, full dependency audit, A/B and opt-in acceptance builds.
- O: `python3 scripts/tests/operations.py`. Actual owned runner / service signals, command failure, foreign listeners and dedicated Simulator lifecycle. Expected failing child commands are assertions, not skipped tests.
- I2: `scripts/verify simulator-stage2-outcomes`. Production A/B then test-only React outcome page using production client/interpreters in installed production shell, real WebKit/native URLSession/backend; includes actual backend-off probe.
- I3: `scripts/verify simulator-stage3-trust-limits`. Production A/B then test-only raw bridge trust/limits probes in installed shell. Deliberately bypasses TS validation, never native policy. Frame attempts here are CSP prevention, not adapter provenance.
- W: `scripts/verify simulator-stage4-webkit-privacy`. Real WKWebView component host + unchanged production adapter/executor with opt-in fixture; no installed React claim. Eleven live tests plus 42 inherited core/adapter tests. Controlled outer frame-navigation bypass and held framework callbacks are explicit test seams.
- I5: `scripts/verify simulator-stage5-production-ux`. Production A/B and actual production Diagnostics UI, no acceptance page or JS injection.
- B: `xcodebuild -project ios/BridgeLab.xcodeproj -scheme BridgeLab -destination 'generic/platform=iOS Simulator' -derivedDataPath "$PWD/.artifacts/final-build" CODE_SIGNING_ALLOWED=NO build analyze`. Universal Simulator build/static analyzer; no signing or real device.

I2, I3 and I5 each run `SameBinaryTests.testWebOnlyUpdateOnSameInstalledApplication`, include the entire Stage1 A/B assertion/manifest/backend procedure and run the 42 inherited Simulator tests. A separate Stage1 rerun is therefore redundant, not missing. None of these commands is itself final product acceptance. All runner outputs are isolated under ignored `.artifacts/verify-*`; commit compact evidence before worktree retirement.

## Requirement → scenario → executable assertion → status / limits

Path shorthand: N = `native/TransportPackage/Tests/TransportPackageTests/`; C = `ios/Tests/BridgeLabCoreTests/`; Web = `web/src/`; BT = `backend/tests/`. Method names below are actual source symbols, not planned identifiers. Status “covered” means an executable assertion exists at the named layer; final exact-head PASS must be read from the observed evidence record, not inferred from this table.

| Requirement and all OpenSpec scenarios | Actual tests / commands | Layer / status / remaining limit |
| --- | --- | --- |
| HT-01 Get query and post opaque body; Text body | N/HTTPExecutorTests.testGETSuccessPreservesOpaqueResponse; N/RequestValidationTests.testPOSTTransportsOpaqueUTF8Body; Web/scenarios.test.ts request construction; BT/fixtures.test.mjs catalog/quote/echo; I2/I3/I5 A/B and I3 Unicode echo | Covered: deterministic URLRequest preservation plus real installed GET/POST and opaque text. Native production source audit excludes business registry/models. |
| HT-02 HTTP failure; Malformed JSON; Connection failure | N/ContractBoundaryTests.testNonRedirectHTTPStatusesPreserveOpaqueText; N/ResponsePolicyTests.testNonemptyResponseRequiresAllowedTextualMediaType; I2 HTTP503/422/malformed/business/backend-off; I5 production errors; W/testActualDecodedGzipHeaderBoundsEmptyHTTPAndEscapedLargeReply | Covered: actual HTTP/JSON/transport distinction, real empty204 without Content-Type. 304 is deterministic executor status classification, not fabricated real wire body evidence. |
| HT-03 Denied target | N/RequestValidationTests.testInvalidRequestFieldsPreventNetworkStart and testDeniedOriginsPreventNetworkStart; N/ContractBoundaryTests.testExactRequestBoundsAndOneByteExcess and testSharedSemanticTransportVectors; I3 trust.ts | Covered: grammar, alternate numeric/IPv6/trailing-dot host, explicit port, GET body/POST content type, header lowercase/value/control, URL8192/+1 and header128/+1. Exhaustive grammar boundaries use executor doubles and assert zero network starts; selected actual raw WebKit denials corroborate no forbidden backend hits. No DNS required. |
| HT-03 Redirects | N/ResponsePolicyTests redirect methods; N/URLSessionNetworkClientTests.testRedirectDelegateReturnsNoDestinationRequest; I3 same302/cross302/loop307; W/testActualRedirectStatusAndLocationMatrixNeverRequestsDestination | Covered: real URLSession all five statuses × valid/missing/malformed Location and zero destination hits; real same/cross/loop zero hits in I3. Unit fallback also checks terminal status when delegate is absent. |
| HT-04 Cookie/header isolation; Authentication challenge | N/URLSessionNetworkClientTests configuration/challenge tests; W/testSyntheticWebNativeCookiesCacheAndChallengeIsolation | Covered actual synthetic native/WK positive controls, dedicated-session no cookie/credential/cache reuse, no Set-Cookie/X-Private exposure/persistence, actual two401 challenges with no Authorization/retry; production model nonpersistent store. No real account/keychain access. |
| HT-05 Boundaries (input/time/concurrency) | N/ContractBoundaryTests exact URL/body/id/header/time bounds; N/RequestValidationTests invalid values; C/BridgeEngineTests numeric/structural timeout tests; I3 raw131072/+1, emoji65536/+1, eight live/ninth BUSY | Covered with proper layers: min1/max30000 deadline admission uses inert scheduler, not a guarantee a real1ms network response succeeds. Deterministic engine tests preserve precedence/consumption and prevent numeric traps. Real WebKit tests cover raw byte bounds and concurrency admission. |
| HT-05 Boundaries (output); Chunked oversize; Unsupported response | N/ResponsePolicyTests data/header/media/UTF8 tests; N/IngestionRegressionTests bounded relay/cancel-at-first-excess; I3 exact1MiB/+1 chunked, binary/invalidUTF8; W/testActualDecodedGzipHeaderBoundsEmptyHTTPAndEscapedLargeReply | Covered: actual HTTP gzip decoded1MiB/+1, exposed8192/+1 with private9KB excluded, actual escaped structured reply above incoming cap. Relay memory and first-excess cancellation checked deterministically; OS/framework header/decompressor internal buffering is not measured or guaranteed. |
| HT-05 Native deadline | N/DeadlineTests injected and real monotonic timer; W/testActualTrickleDeadlineStopsSocketWithoutJavaScriptTimer; I2/I5 native timeout | Covered: real continuing50ms body chunks, native500ms terminal, unfinished socket closure; no JS timer. Native clock/actor seams prove independence from JS scheduling; no artificial OS suspension claim. |
| HT-06 Cancel wins; Response wins; Concurrent responses | N/CancellationAndConcurrencyTests and AdmissionTests; C/WKBridgeAdapterTests terminal/publication ordering; I2 explicit true/false cancel and fast/slow; I5 cancel isolation | Covered: exact native receipt/first-terminal tests plus actual socket-close/no-late-success and correlated installed UI responses. Executor selection is authoritative, not network-emission wall-clock order. Cancellation cannot roll back server effects. |
| WB-01 Unsupported version; Malformed input | C/BridgeEngineTests numeric/version/raw/shape/session tests; I3 raw wire matrix; U protocol/v1/validate.cjs | Covered: actual engine and installed WebKit error codes, recoverable/null id, no admission on malformed input. Schema36 classifications alone do NOT execute semantic errors; N/ContractBoundaryTests executes six transport semantic vectors, adapter owns session denial. |
| WB-02 Origin and iframe denial; No external navigation | C/BridgePolicyTests; W/testRealFrameMetadataRejectedAtAdapterAfterTestOnlyNavigationBypass, testRealMainFrameUncommittedAndInactiveMessagesDenied, testActualDeniedNavigationFrameAndNewWindowKeepCurrentDocument | Covered real securityOrigin/isMainFrame metadata, actual main/frame delegate denials and zero destination hits. Held real commit and explicit inactive callback are ordering seams. New window is rejected by WebKit without production WKUIDelegate, not necessarily a nil-target adapter callback. Production CSP is unchanged. |
| WB-03 Duplicate request; Document handshake/stale token | C/BridgeEngineTests shape/session/high-water methods; C/WKBridgeAdapterTests queued hello/request/cancel; I3 active/completed duplicates/repeated hello; W live same-URL reload | Covered: real fresh token/id1 and old-token denial; deterministic queued ingress checks preserve high-water and old original. TS allocator exhaustion uses explicit Reflect test seam (not two billion calls); adapter max-id is separately bounded in engine tests. |
| WB-03 Safe text; Cancel acknowledgement | I3 hostile echo and inert React text; W escaped large reply; C/BridgeEngineTests.testResponsePreservesSafeTextAsStructuredData and testCancelAcknowledgesOnlyExecutorWinner; Web/bridgeClient.test.ts malformed/mismatched response/hello/cancel tests | Covered real structured reply delivery and independent original/cancel Promise results. Production hostile error panel is separately DOM/mock-tested, not mislabeled hostile-input installed UI evidence. |
| WB-04 Reload id reuse; Teardown | W live reload/navigation/provisional-failure/close/destruction tests; C/WKBridgeAdapterTests.testEveryRevocationEventCancelsAdmittedTaskAndDeadlineExactlyOnce, testOldNetworkAndDeadlineAfterFreshIDOneCannotTouchFreshTask, queued/admission/publication tests | Covered actual socket closure, old-page cancellation, handler/weak-owner release and wait beyond old delay. Actual OS WebContent termination is NOT induced: real adapter delegate method + deterministic active-task/deadline/reply retirement tests cover that event. Reviewer must judge this explicit framework-callback seam, not infer an OS kill experiment. |
| WS-01 Catalog A; Quote B | I2/I3/I5 production A/B; Web/scenarios.test.ts; BT catalog/quote valid/empty/invalid quantity | Covered real Notebook/total1 then nested quote1200 and native-attributed backend methods. Empty category and invalid quantity fixture edges have backend/interpreter and I2 coverage; not every edge is a production control UI test. |
| WS-02 Error categories; Missing bridge; Concurrent cancel UI | I5 production Diagnostics HTTP/business/JSON/timeout/loading/fast+slow cancellation; I2 actual backend-off NETWORK_ERROR; Web/App.test.ts and bridgeClient.test.ts no-fetch, hostile text, pagehide | Covered actual production UI where requested; missing bridge/no fetch and malformed native replies require DOM/boundary tests. Human cancel window10s with native15s deadline. No VoiceOver/physical-device acceptance claim. |
| WS-03 Web-only update | I2/I3/I5 SameBinaryTests + scripts/verify installed/require_same_app | Covered full sorted regular-file manifest (hidden included, symlinks fail closed), same container/UDID/executable before/after A/B/probes, changed web hashes and actual visible endpoints/results. No native build/install/relaunch between A/B. Hash equality is within run, not reproducible binary hashes across build paths. |
| LL-01 Static replacement; Fixture coverage; Safe asset path | BT/fixtures.test.mjs, arguments.test.mjs, static-cleanup.test.mjs; I2/I3/I5 web rebuild | Covered real ephemeral HTTP: all architecture routes/invalid input/CSP/no-CORS/no-store/MIME, absent assets503 with healthy listener, traversal/symlink denial, file descriptors closed on abort/shutdown. |
| LL-02 Repeat/conflict; Stale/partial startup; Shutdown active work | BT/lifecycle.test.mjs and port-retry tests; O; scripts/tests/test_process_cleanup.py; each Simulator runner finally | Covered real CLI from other cwd, locking/idempotence/concurrency, stale/PID-reuse/mismatch, bind rollback, verified foreign listener survives, actual SIGINT/SIGTERM/failing command/timeout descendant cleanup and delayed socket closure. Only owned groups/state/UDIDs. No SIGKILL/crash-finally, session-escaping descendant, malicious same-user forgery or instruction-level spawn/state-write atomicity guarantee. |
| LL-03 Real acceptance; Lifecycle closure | U/O/I2/I3/W/I5/B exact-source evidence; reviewed parent handoffs; same-card independent iosverifier then separate acceptance and coordinator gates | Implementation candidate evidence only. Independent integration review, separate exact-RC acceptance, owner RC decision and archive remain pending until those actors record them. Unchecked tasks cannot be hidden by OpenSpec parser success. |

## Reviewed inputs and history

Parent approvals: iOS `15a9b53698357fc9e4a828f045e079d9644710de`, web `6936e00938b92c617e975784b5b776dd95247870`, backend `4e4474f1e39dda7ad6e0f421eee9dc49583b0229`. All are preserved ancestors. iOS includes reviewed foundation `e9f338142735b754362c59ed62ad1fa8450c0b4e` and transport `b5c5423c585b8599f5639075e5c40c8a5f517ada`, plus acknowledged admission/terminal-drain and numeric fixes. Full prior review dispositions are in parent Kanban completion metadata; source evidence lives in module documentation. New final reconciliation tests do not change production native policy, backend or client semantics.

Stage5 corrected an XCUI text-node assumption (not proven a timing-only bug) and exposed/fixed owned command grandchild leaks. The coordinator's combined external420s timeout required manually recovering only its recorded Simulator; that was NOT product auto-cleanup. Existing runner group cleanup and fixture ownership require independent review of the entire accumulated integration diff, not only this final slice. Historical Stage4 mutation controls prove selected tests detect removed frame/revoke/cookie guards, not universal security.

No final acceptance, primary-main merge, remote publication, OpenSpec archive, graph/profile/runtime changes or new agents/cards are performed by this implementation handoff. Simulator/synthetic loopback only; not production-ready.
